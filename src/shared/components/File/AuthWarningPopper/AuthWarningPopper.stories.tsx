import { AuthWarningPopper } from './AuthWarningPopper';

const Meta = {
  title: 'components/File/AuthWarningPopper',
  component: AuthWarningPopper,
};

export const Open = () => <AuthWarningPopper isOpen anchorElement={null} />;

export default Meta;
