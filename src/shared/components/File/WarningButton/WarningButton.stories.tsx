import { WarningButton } from './WarningButton';

const Meta = {
  title: 'components/File/WarningButton',
  component: WarningButton,
};

export const Default = () => <WarningButton onClick={() => {}} />;

export default Meta;
