import {
  reducer,
  selectData,
  selectStatus,
  selectError,
  getData,
} from './redux/slice';
import { Card } from './Card';

export { Card, reducer, selectData, selectStatus, selectError, getData };
