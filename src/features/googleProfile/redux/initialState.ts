import { State } from '../types';

const initialState: State = {
  avatarURL: '',
  fullName: 'Unknown',
  dateOfBirth: null,
  email: null,
  phoneNumber: null,
  status: 'idle',
  error: null,
};

export { initialState };
